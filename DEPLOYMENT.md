# 部署指南

## 📦 项目构建

在部署之前，确保项目已经构建完成：

```bash
npm run build
```

构建完成后，`dist` 目录将包含所有需要部署的文件。

---

## 🚀 部署方式

### 方式一：Surge.sh 部署（推荐，用户之前使用过）

#### 1. 安装 Surge（如果还没有安装）

```bash
npm install -g surge
```

#### 2. 登录 Surge

```bash
surge login
```

#### 3. 部署到 Surge

**方法A：交互式部署（推荐）**

```bash
cd dist
surge
```

然后按照提示输入：
- `project:` - 直接按 Enter（使用当前目录 `dist`）
- `domain:` - 输入你想要的域名，例如：`700-yuan-romance`（不要加 `.surge.sh` 后缀）

**方法B：直接部署（一行命令）**

```bash
cd dist
surge . 700-yuan-romance.surge.sh
```

或者：

```bash
surge dist 700-yuan-romance.surge.sh
```

#### 4. 更新部署

如果之后修改了代码，重新构建后：

```bash
npm run build
cd dist
surge . 700-yuan-romance.surge.sh
```

#### 5. 查看部署状态

```bash
surge list
```

#### 6. 删除部署

```bash
surge teardown 700-yuan-romance.surge.sh
```

---

### 方式二：Vercel 部署

#### 1. 安装 Vercel CLI

```bash
npm install -g vercel
```

#### 2. 登录 Vercel

```bash
vercel login
```

#### 3. 部署

在项目根目录运行：

```bash
vercel
```

或者直接部署到生产环境：

```bash
vercel --prod
```

#### 4. 配置说明

项目已经包含 `vercel.json` 配置文件：

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

这个配置确保：
- 构建命令正确
- 输出目录正确
- SPA路由正常工作

---

### 方式三：Netlify 部署

#### 1. 安装 Netlify CLI

```bash
npm install -g netlify-cli
```

#### 2. 登录 Netlify

```bash
netlify login
```

#### 3. 部署

```bash
netlify deploy --prod --dir=dist
```

#### 4. 首次部署需要初始化

```bash
netlify init
```

---

### 方式四：GitHub Pages 部署

#### 1. 安装 gh-pages

```bash
npm install --save-dev gh-pages
```

#### 2. 在 package.json 中添加脚本

```json
{
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

#### 3. 部署

```bash
npm run deploy
```

#### 4. 在 GitHub 仓库设置中启用 GitHub Pages

- Settings → Pages
- Source: `gh-pages` branch
- 保存

---

## 🔧 部署前检查清单

- [ ] 运行 `npm run build` 确保构建成功
- [ ] 检查 `dist` 目录是否包含所有文件
- [ ] 测试本地预览：`npm run preview`
- [ ] 检查 `index.html` 是否正确
- [ ] 确认所有资源路径正确（相对路径）

---

## 📝 常见问题

### 1. Surge 部署时提示 "please enter valid project path"

**问题**：在 `project:` 提示时输入了域名或其他内容

**解决**：
- `project:` 应该输入目录路径（如 `.` 或 `dist`），或直接按 Enter
- `domain:` 才输入域名（如 `700-yuan-romance`，不要加 `.surge.sh`）

### 2. 页面空白或404

**问题**：路由配置不正确

**解决**：
- 确保部署平台支持 SPA 路由重写
- Surge：创建 `dist/_redirects` 文件，内容：`/* /index.html 200`
- Vercel：已配置 `vercel.json`
- Netlify：创建 `dist/_redirects` 文件

### 3. 资源加载失败

**问题**：资源路径不正确

**解决**：
- 检查 `vite.config.ts` 中的 `base` 配置
- 确保使用相对路径：`base: './'`

---

## 🎯 推荐部署流程（Surge）

```bash
# 1. 构建项目
npm run build

# 2. 进入构建目录
cd dist

# 3. 部署（交互式）
surge

# 或者直接部署（非交互式）
surge . 700-yuan-romance.surge.sh

# 4. 访问你的网站
# https://700-yuan-romance.surge.sh
```

---

## 📊 部署后验证

部署完成后，请验证：

1. ✅ 首页正常加载
2. ✅ 游戏可以正常开始
3. ✅ 所有选择按钮正常工作
4. ✅ 背景图片正常加载
5. ✅ 文字显示正常
6. ✅ 路由跳转正常（如果有）

---

## 🔄 更新部署

每次更新代码后：

```bash
# 1. 重新构建
npm run build

# 2. 重新部署
cd dist
surge . 700-yuan-romance.surge.sh
```

---

## 📞 需要帮助？

如果遇到部署问题：

1. 检查构建日志是否有错误
2. 检查部署平台的日志
3. 确认所有依赖都已安装
4. 确认环境变量（如果有）已正确配置

---

**最后更新**：2024年




